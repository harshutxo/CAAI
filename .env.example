# Copy this file to .env and fill in your key to enable AI-powered document
# extraction (`casuite doc extract`). Without it, extraction falls back to
# local OCR + heuristics automatically — the tool still works fine offline.
ANTHROPIC_API_KEY=sk-ant-...
